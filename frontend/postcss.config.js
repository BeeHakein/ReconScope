/**
 * PostCSS configuration for the ReconScope frontend.
 *
 * Applies Tailwind CSS for utility-class generation and Autoprefixer
 * for cross-browser vendor-prefix support.
 *
 * @see https://postcss.org/
 */
export default {
  plugins: {
    tailwindcss: {},
    autoprefixer: {},
  },
};
